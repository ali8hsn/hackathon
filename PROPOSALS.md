# Proposals Log

If you need to change `schema.prisma` or the API contract in `PROJECT_BRIEF.md §7`, append a proposal here FIRST, then code the minimal interpretation of the original contract. A human will accept or reject the proposal later.

Format:

```
## [ISO timestamp] WS-<letter>: <one-line title>
**What the spec says now:** quote the relevant lines.
**Why I want to change it:** 1-2 sentences.
**Proposed new version:** exact text that should replace the current spec.
**Impact:** which workstreams / endpoints / components would have to change.
```

---

(no proposals yet)
